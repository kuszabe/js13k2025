## Submission for the 2025 js13k competition
## Development
run `esbuild main.js --bundle --outfile="dist/main.js" --watch --format=esm --sourcemap` to build the files and use an http server to serve ./dist
## how to build
run the ./build script
It minifies the js and svgs then bundles the into output.zip
